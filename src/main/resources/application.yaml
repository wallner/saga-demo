spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:file:./camunda-h2-database;DB_CLOSE_ON_EXIT=false
    hikari:
      username: sa
  h2:
    console:
      enabled: true
camunda.bpm.admin-user:
  id: demo
  password: demo

logging:
  level:
    org:
      javamoney:
        moneta:
          FastMoney: WARN
          Money: WARN